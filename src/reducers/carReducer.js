export const initialState ={

}

export const carReducer = (state, action) =>{
    switch(action.type){
        case 'placeholder':
            return state;
        default:
            return state;
    }
}